# Netlify публикует ТОЛЬКО статический сайт из папки webapp.
# base = webapp -> Netlify заходит в webapp и не видит requirements.txt в корне,
# поэтому не пытается ставить Python-зависимости. Сборки нет — просто раздаём файлы.
[build]
  base = "webapp"
  publish = "."
  command = ""
